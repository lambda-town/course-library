import lambda.utils.Emojis.robot

object Main extends App {
  //[userInput
    object Main extends App
  //]

  Main.main(Array.empty)

  println(
    s"""
      |$robot Congrats!
      |Keep in mind that immutable bindings (val) should be preferred, and mutable variables only used when necessary.
      |""".stripMargin)
}
