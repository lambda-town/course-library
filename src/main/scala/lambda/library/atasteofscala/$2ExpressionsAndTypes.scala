package lambda.library.atasteofscala

object $2ExpressionsAndTypes {

  val pageId = s"$id-expressions-and-types"

  def apply() =
    simplePage("Expressions and Types")
      .withWidget(mk(widgetId(pageId, 0), "TODO"))
}
